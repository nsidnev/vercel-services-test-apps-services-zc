[project]
name = "service-fastapi"
version = "0.1.0"
description = "Service FastAPI - demonstrates service-to-service communication (apps-services layout)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.128.0",
    "httpx>=0.28.0",
    "uvicorn>=0.40.0",
]
